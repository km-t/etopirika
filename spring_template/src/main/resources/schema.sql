/*
 DROP TABLE IF EXISTS shops;

CREATE TABLE shops
(
  id INTEGER PRIMARY KEY AUTO_INCREMENT,
  urls varchar(256),
  name varchar(256),
  budget_average varchar(256),
  catch varchar(256),
  close varchar(256),
  genre varchar(256),
  large_area varchar(256),
  large_service_area varchar(256),
  lunch varchar(256),
  middle_area varchar(256),
  open varchar(256),
  service_area varchar(256),
  small_area varchar(256),
  station_name varchar(256),
  photo varchar(256)
);
*/